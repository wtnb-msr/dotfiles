# Claude Code tmux worktree management
# Source the tmux-claude-worktree script

if [ -f "${HOME}/.claude/scripts/tmux-claude-worktree.sh" ]; then
    source "${HOME}/.claude/scripts/tmux-claude-worktree.sh"
fi
